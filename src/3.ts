const solveMathProblem = () => {
  const problem1 = Math.random() * 10;
  const problem2 = Math.random() * 10;
  const sum = problem1 + problem2;
  return `The solution to the math problem is ${sum}.`;
}
